This is just an hello world with alloy/titanium that perform an connection to a server 
that run in localhost (the developer machine) with a base authentication

to let the script run chage:
1. username
2. password
3. path 

in the index.js file.
The script should work either in IOS and Android

